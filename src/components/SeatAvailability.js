import React from "react"
import Seat from './Seat'

const SeatAvailability = () => {
	return (
		<div className="row">
			Unoccupied : ? {/*Set the Seat component color using props  seat-white and seat-black*/}
			Occupied : ?
		</div>
	)
}

export default SeatAvailability
